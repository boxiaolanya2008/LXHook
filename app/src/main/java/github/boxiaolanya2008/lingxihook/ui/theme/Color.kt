package github.boxiaolanya2008.lingxihook.ui.theme

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.graphics.toArgb

/** 自定义主色选项（移植自 KernelSU KernelUI） */
val keyColorOptions = listOf(
    Color(0xFFF44336).toArgb(), // 红
    Color(0xFFE91E63).toArgb(), // 粉
    Color(0xFF9C27B0).toArgb(), // 紫
    Color(0xFF673AB7).toArgb(), // 深紫
    Color(0xFF3F51B5).toArgb(), // 靛蓝
    Color(0xFF2196F3).toArgb(), // 蓝
    Color(0xFF00BCD4).toArgb(), // 青
    Color(0xFF009688).toArgb(), // 蓝绿
    Color(0xFF4FAF50).toArgb(), // 绿
    Color(0xFFFFEB3B).toArgb(), // 黄
    Color(0xFFFFC107).toArgb(), // 琥珀
    Color(0xFFFF9800).toArgb(), // 橙
    Color(0xFF795548).toArgb(), // 棕
    Color(0xFF607D8F).toArgb(), // 蓝灰
    Color(0xFFFF9CA8).toArgb()  // 浅粉
)